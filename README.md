# Patch FalconSensor for ACA
Flexible pipeline for DevOps that will leverage a dockerbuild file or a public image, patch the image with the falcon sensor, scan the image for vulnerabilities and deploy an Azure Container App Service 
